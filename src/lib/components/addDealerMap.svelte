<!--leaflet Map-->
<div class="content box">
    <div id="map" style="width: 100%; height: 600px;"></div>
  </div>

<link rel="stylesheet" href="https://unpkg.com/leaflet@1.9.4/dist/leaflet.css" />
<script>
  // const map = L.map('map').setView([53.517073, -7.5840399], 7);
  // L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', {
  //  maxZoom: 19,
  //    attribution: '© OpenStreetMap'
  // }).addTo(map); 

const map = L.map('map').setView([53.526948, -7.586303], 8);

	const tiles = L.tileLayer('https://tile.openstreetmap.org/{z}/{x}/{y}.png', {
		maxZoom: 19,
		attribution: '&copy; <a href="http://www.openstreetmap.org/copyright">OpenStreetMap</a>'
	}).addTo(map);

	const popup = L.popup()
		.setLatLng([53.47497, -7.624512])
		.setContent('Double Click map </br>to add Geo co-ordinates </br>in add delaer form.')
		.openOn(map);

	function onMapClick(e) {
		popup
			.setLatLng(e.latlng)
			.setContent(`Co-ordinates ${e.latlng.toString()}`)
			.openOn(map);
	}

	map.on('click', onMapClick);
// Sets map view to average lat and lng
// map.setView([53.526948, -7.586303], 8);
map.doubleClickZoom.disable();

import { LeafletMouseEvent } from 'leaflet';

map.on('dblclick', function(ev) {
  let latMaDbClick = ev.latlng.lat.toFixed(6);
  let lngMaDbClick = ev.latlng.lng.toFixed(6);
const latitudeElement = document.getElementById('latitude');
if (latitudeElement) {
    latitudeElement.value = latMaDbClick;
}
const longitudeElement = document.getElementById('longitude');
if (longitudeElement) {
    longitudeElement.value = lngMaDbClick;
}
});
</script>